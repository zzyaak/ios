# Быстрый старт iOS приложения

## ✅ Что уже сделано:

1. ✅ Создан файл `index.html` в папке `Proskomidiya/Proskomidiya/Resources/`
2. ✅ Обновлён `WebViewController.swift` для загрузки локального HTML
3. ✅ Настроен офлайн режим

## 📋 Что нужно сделать в Xcode:

### Шаг 1: Добавить index.html в проект

1. Откройте проект в Xcode:
   ```bash
   open Proskomidiya.xcodeproj
   ```

2. В левой панели (Navigator) найдите папку `Resources` (или создайте её, если её нет)

3. **Правой кнопкой мыши** на папке `Resources` → **Add Files to "Proskomidiya"...**

4. Выберите файл `Proskomidiya/Proskomidiya/Resources/index.html`

5. **ВАЖНО!** Убедитесь, что отмечены:
   - ✅ **"Copy items if needed"** (если файл не в папке проекта)
   - ✅ **"Add to targets: Proskomidiya"**

6. Нажмите **Add**

### Шаг 2: Проверить настройки файла

1. Выберите `index.html` в навигаторе
2. В правой панели (Inspector) проверьте:
   - **Target Membership**: должен быть отмечен `Proskomidiya`
   - **Location**: должен быть указан правильный путь

### Шаг 3: Запустить приложение

1. Выберите симулятор или подключите устройство
2. Нажмите **▶️ Run** (Cmd+R)
3. Приложение должно загрузить локальный HTML без интернета!

## 🔧 Если файл не загружается:

### Проверка 1: Файл в Bundle
Убедитесь, что файл добавлен в Target:
- Выберите проект в навигаторе
- Выберите Target "Proskomidiya"
- Вкладка **Build Phases** → **Copy Bundle Resources**
- Должен быть `index.html`

### Проверка 2: Путь к файлу
В консоли Xcode (нижняя панель) при запуске должно быть:
```
✅ Загружен локальный HTML из Bundle
```

Если видите:
```
⚠️ Локальный HTML не найден, загружаем из интернета
```
Значит файл не добавлен в Bundle правильно.

### Проверка 3: Ручное добавление в Build Phases
1. Выберите проект → Target "Proskomidiya"
2. Вкладка **Build Phases**
3. Раскройте **Copy Bundle Resources**
4. Нажмите **+**
5. Выберите `index.html`
6. Нажмите **Add**

## 📱 Тестирование

1. **Офлайн режим**: Отключите Wi‑Fi и сотовую связь → приложение должно работать
2. **Онлайн режим**: Если удалить `index.html` из Bundle, приложение загрузит сайт из интернета

## 🎯 Особенности

- ✅ Работает **без интернета**
- ✅ Быстрая загрузка (локальный файл)
- ✅ Гибридный режим: если локальный файл не найден → загрузка из интернета
- ✅ Все функции форм работают (выбор срока, количества молитв, ввод имён)

## 📝 Обновление контента

Чтобы обновить HTML:
1. Отредактируйте `Proskomidiya/Proskomidiya/Resources/index.html`
2. Пересоберите приложение (Cmd+B)
3. Запустите заново

---

**Готово!** Приложение готово к использованию офлайн! 🎉

