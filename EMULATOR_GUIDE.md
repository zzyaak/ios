# Инструкция по запуску iOS приложения на эмуляторе

## Быстрый старт

### Шаг 1: Открыть проект в Xcode
```bash
cd /Users/ilya/Desktop/proskomidiyaa-ios
open Proskomidiya.xcodeproj
```

Или:
1. Откройте Xcode
2. File → Open → выберите `Proskomidiya.xcodeproj`

### Шаг 2: Выбрать эмулятор

В Xcode в верхней панели:
1. Нажмите на **схему проекта** (рядом с кнопкой Run ▶️)
2. Выберите **iPhone** (например, iPhone 15, iPhone 15 Pro, iPhone 14 и т.д.)
3. Если эмуляторы не загружены, выберите **Window → Devices and Simulators → Simulators** → нажмите **+** для добавления

### Шаг 3: Запустить приложение

1. Нажмите кнопку **▶️ Run** (или нажмите `Cmd + R`)
2. Xcode соберёт проект и запустит эмулятор
3. Приложение откроется в эмуляторе

## Полезные команды

### Запуск из терминала
```bash
cd /Users/ilya/Desktop/proskomidiyaa-ios
xcodebuild -project Proskomidiya.xcodeproj -scheme Proskomidiya -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' build
```

### Запуск через xcrun
```bash
xcrun simctl list devices available
xcrun simctl boot "iPhone 15"
```

## Управление эмулятором

- **Rotate**: `Cmd + ←` или `Cmd + →`
- **Home**: `Cmd + Shift + H`
- **Screenshot**: `Cmd + S`
- **Закрыть**: `Cmd + Q` (в Xcode)

## Возможные проблемы

### Проблема: "No such module 'UIKit'"
**Решение**: Убедитесь, что выбрана правильная схема (Scheme) - `Proskomidiya`

### Проблема: "Simulator not found"
**Решение**: 
1. Window → Devices and Simulators
2. Добавьте нужный симулятор через "+"

### Проблема: "Code signing error"
**Решение**: 
1. Выберите проект в навигаторе
2. Target → Signing & Capabilities
3. Выберите команду разработчика или "Automatically manage signing"

### Проблема: Приложение не запускается
**Решение**:
1. Product → Clean Build Folder (`Cmd + Shift + K`)
2. Product → Build (`Cmd + B`)
3. Запустите снова (`Cmd + R`)

## Проверка работы

После запуска вы должны увидеть:
- ✅ WebView загружает веб-сайт или локальный HTML
- ✅ Навигационная панель вверху
- ✅ Прогресс-бар при загрузке
- ✅ Тулбар с кнопками внизу

## Отладка

### Консоль Xcode
Внизу Xcode есть панель консоли - там будут выводиться:
- `✅ Загружен локальный HTML из Bundle` - успешная загрузка локального файла
- `⚠️ Локальный HTML не найден, загружаем из интернета` - загрузка из сети

### Safari Web Inspector
1. Safari → Develop → Simulator → Ваше приложение
2. Откроется Web Inspector для отладки WebView

## Тестирование функций

1. **Навигация**: Используйте кнопки "Назад" и "Вперёд" в тулбаре
2. **Обновление**: Нажмите кнопку обновления
3. **Главная**: Нажмите кнопку "Домой" для возврата на главную
4. **Офлайн режим**: Отключите интернет в настройках эмулятора - приложение должно загрузить локальный HTML

## Настройки эмулятора

1. **Settings → General → Software Update** - для обновления iOS
2. **Settings → Safari → Clear History** - для очистки кеша
3. **Hardware → Device** - для выбора устройства

---

**Готово!** Приложение должно успешно запуститься на эмуляторе! 🎉

